# Telegram Integration

`run-weekly` creates Telegram reminder drafts by default.

It sends messages only when `--send-telegram` is passed.

## Commands

```bash
pmflow telegram-reminders --actions memory/actions/product-sync-actions.md
pmflow telegram-send --message "Hello"
```
